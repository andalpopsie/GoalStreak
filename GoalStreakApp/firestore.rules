rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary permissive rules for testing - authenticated users can access their data
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}